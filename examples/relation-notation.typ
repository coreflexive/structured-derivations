#import "../lib.typ": *

#set page(
  width: auto,
  height: auto,
  margin: 12pt,
)

#set text(size: 13pt)

$ reltype(X, Y) $

$ relid(X) $

$ reltop(X, Y) $

$ relbot(X, Y) $

$ relconverse(R) $

$ relcomplement(R) $

$ relimage(R, A) $
